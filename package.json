{
  "name": "js_source_code",
  "version": "0.2.0",
  "private": true,
  "license": "ISC",
  "project_src": "./example.json",
  "dependencies": {
    "rsync": "^0.6.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "react-scripts build && node deploy",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
